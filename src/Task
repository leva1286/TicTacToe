задача создать игру крестики нолики.
1 создайте абстрактный класс Sign(знак) и добавте в него строковое поле symbol (символ);
сделайте конструктор принимающий это поле и реализуйте абстрактный
метод canMoveToPosition принимающий PlayingField playingField, int line, int column и возвращающий boolean значение
и реализуйте абстрактный метод getSymbol возвращающий строку.

2 создайте класс PlayingField
в нем создайте следующии поля:
  двойной строковый массив field и инициализирузте его так Sign[3][3];
  целочисленную переменную strokeNumber равную 1;
  строкувую переменную nowPlayer
  потом создайте конструктор принимающий nowPlayer

3 откройте задание про шахматы и перепешите от туда методы moveToPosition и printField
(исправив под себя) в класс PlayingField;

4 создайте класс Main и напишите его переделав класс Main из проекта шахмат;

5 создайте класс Cross и Zero и унаследуйте их от класса Sign
    сделайте им конструкторы;
    и методы canMoveToPosition, getSymbol
    метод canMoveToPosition должен возвращать false  если клетка куда
указывают координаты уже занята;
    метод getSymbol должен возвращать 'X' для Cross
    и 'O' для Zero

6 создайте в классе PlayingField метод isVictory который будет возвращать true когда
    игрок победил и добавте его в main чтобы он вызывался.